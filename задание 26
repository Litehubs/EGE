#ТИП СКИДКИ
1)
from math import *
f = open('26.1.txt')
n = int(f.readline())
a = [int(s) for s in f]                     #Введем все данные
a120 = [x for x in a if x > 120]
a120.sort()                                 # по возрастанию
sale = sum(a120[:len(a120)//2])*0.25
print(ceil(sum(a) - sale), max(a120[:len(a120)//2]))
ОТВЕТ: 472707 563

2)from math import *
f = open('26.2.txt')
n = int(f.readline())
a = [int(s) for s in f]
a.sort(reverse=True)             #по убыванию
sale1 = sum(a[:2500])*0.5
a.sort()                         #по возрастанию
sale2 = sum(a[:2500])*0.5
print(sum(a)- sale1, sum(a)- sale2)
ОТВЕТ : 3930667 4868067

3)from math import *
f = open('26.3.txt')
n = int(f.readline())
a = [int(s) for s in f]
a.sort(reverse=True)             #по убыванию
sale1 = sum(a[:n//3])
print(sum(a) - sale1)
sale2 = 0
for i in range(n):
    if (i + 1) % 3 == 0:
        sale2 += a[i]
print(sum(a) - sale2)


#ТИПЫ НА КОРОБКИ

1)f = open('Задание 2.txt')
n = int(f.readline())
boxes = [int(s) for s in f]
boxes.sort(reverse=True)    #сортирую в порядке убывания
gift = [boxes[0]]
for box in boxes[1:]:
    if gift[-1] - box >=5:      #условие для добавления коробки
        gift.append(box)
print(len(gift), min(gift))

2)f = open('26.3.txt')
n = int(f.readline())
boxes = []
for s in f:
    lens, color = s.split()
    boxes.append([int(lens),color])
boxes.sort(reverse=True)
bkocks = []
while boxes != []:
    gift = [boxes[0]]
    del boxes[0]
    for box in boxes[:]:
        if gift[-1][0] - box[0] >= 8 and gift[-1][1] != box[1]:
            gift.append(box)
            boxes.remove(box)
    bkocks.append(gift)
print(len(bkocks), len(bkocks[0]))

3)
