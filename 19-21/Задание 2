def f(a,m):
    if a <= 19: return m % 2 == 0
    if m == 0: return 0
    h = [f(a - 2, m-1),f(a - 5,m -1), f(a //3, m -1)]
    return any(h) if (m - 1) % 2 ==0 else all(h)
print([s for s in range(20,700) if f(s,4) and not f(s,2)])
#19 - 22 60
# 21 - 62 63
# 22 -  64
