1)
f = open('9_1 — копия.txt')
cnt = 0
for s in f:
    a = list(map(int, s.split()))
    if sum(a) / 3 < 30:
        cnt+=1
print(cnt)

2)
f = open('9_1 — копия — копия.txt')
cnt = 0
for s in f:
    a = list(map(int, s.split()))
    if (max(a) + min(a)) % 3 ==0 and \
            (abs(a[0] - a[1]) == abs(a[2] - a[3]) or \
                 abs(a[0] - a[2]) == abs(a[1] - a[3])):
        cnt+=1
print(cnt)
3)
