1)
f = open('9_1 — копия.txt')
cnt = 0
for s in f:
    a = list(map(int, s.split()))
    if sum(a) / 3 < 30:
        cnt+=1
print(cnt)

2)
f = open('9_1 — копия — копия.txt')
cnt = 0
for s in f:
    a = list(map(int, s.split()))
    if (max(a) + min(a)) % 3 ==0 and \
            (abs(a[0] - a[1]) == abs(a[2] - a[3]) or \
                 abs(a[0] - a[2]) == abs(a[1] - a[3])):
        cnt+=1
print(cnt)

3)f = open('444.txt')
cnt = 0
for s in f:
    a = list(map(int, s.split()))
    povt = [ x for x in a if a.count(x) > 1 ]
    npovt = [x for x in a if a.count(x) == 1]
    if ((len(npovt) == 3 and len(set(povt)) ==2)
            and sum(povt) / len(povt) < sum(npovt) / len(npovt)):
        cnt+=1
print(cnt)
4)
f = open('444.txt')
cnt = 0
for s in f:
    a = list(map(int, s.split()))
    povt = [x for x in a if a.count(x) > 1]
    npovt = [x for x in a if a.count(x) ==1]
    if len(povt) == 3 and sum(povt)**2 > sum(npovt)**2:
        cnt+=1
print(cnt)

ДЗ:
1)f = open('3.txt')
cnt = 0
for s in f:
    a = list(map(int, s.split()))
    pv = [x for x in a if a.count(x) > 1]
    npv = [x for x in a if a.count(x) == 1]
    if len(pv) >= 1 and abs(sum(npv)) % 2 !=0:
        cnt+=1
print(cnt)
6)
f = open('6.txt')
cnt = 0
for s in f:
    a = sorted(list(map(int, s.split())))
    if sum(a)/len(a) < 15:
        cnt+=1
print(cnt)

