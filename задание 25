Делители:
1) оптимизация корнем
for x in range(23456,123456 + 1):
    delitel = []
    if x**(1/2) % 1 == 0:
        for d in range(2,x):               #нетривиальный делитель начинается от 2 и до самого числа не включительно
            if x % d ==0:
                delitel.append(d)
    if len(delitel) == 3:
        print(x, max(delitel))
2)cnt = 0
for n in range(800001,800001 + 1000):
    for d in range(17,n,10):
        if n % d == 0:
            print(n,d)
            cnt+=1
            break
    if cnt ==5:
        break


3) cnt = 0
M = 0
for n in range(900001,900001 + 1000):
    delit = []
    for d in range(2, n):
        if n % d ==0:
            M = d + n // d
            break
    if M % 10 ==2:
        cnt+=1
        print(n,M)
    if cnt ==5:
        break

4)cnt = 0
for n in range(6651221,6651221 + 1000):
    deliteli = []
    for d in range(2,(int(n ** 0.5))+1):
         if n % d == 0:
             deliteli.append(d)
             deliteli.append(n // d)
    if len(deliteli) == 2 and  str(deliteli[0]).count('2') ==1 \
        and str(deliteli[1]).count('2') ==1:
        print(n, max(deliteli))
        cnt+=1
    if cnt ==5:
        break

5)палиндром
def f(n):
    for d in range(2, int(n **0.5) +1):
        if n % d ==0:
            return False
    return True

for n in range(5800001,5800001 + 1000):
    delit = []
    for d in range(2, int(n **0.5) +1):
        if n % d ==0:
            if f(d):
                delit.append(d)
            if f(n // d):
                delit.append(n //d)
    if delit:
        M = max(delit) + min(delit)
        if M > 80000 and str(M) == str(M)[::-1]:
            print(n,M)
